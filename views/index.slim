doctype html
html
  head
    title respondto.it: debug and stub webhooks like a pro.
    link href=url("/css/bootstrap.no-icons.min.css") rel="stylesheet"
    link href="//netdna.bootstrapcdn.com/font-awesome/2.0/css/font-awesome.css" rel="stylesheet"
    link rel="stylesheet" href=url("/home.css")
    script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js" type="text/javascript"
    javascript:
      function goToPath() {
        $p = escape($.trim($('#path').val()));
        if ($p === '') { return false; }
        document.location.href='/' + $p + '?view';
        return false;
      };

      $(document).ready(function() {
        $('a#gotopath').click(goToPath);
        $('form#go-path').submit(goToPath);
        $('a#goto-top').click(function() {
          $('body').animate({scrollTop: 0}, {
            duration: 1000,
            complete: function() { $('#path').focus(); }
          });
          return false;
        });
      });
    
    javascript:
      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-361923-6']);
      _gaq.push(['_trackPageview']);
      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();

  body
    a#top
    .container
      .row
        .title.span12
          h1 Debug web hooks like a pro.
      .row
        .title.span8.offset2
          p Create an endpoint to collect and respond to web hooks. View the last several requests, and configure canned responses in both JSON and XML.
      .hero-unit
        table
          tr
            td
              h1
                | respondto.it / 
            td
              form#go-path
                input type="text" name="path" id="path"
            td
              a#gotopath.btn.btn-primary.btn-large GO
      .row
        .span4
          h3 Vanity URLs
          p
            span style='text-decoration: underline;' http://respondto.it/my-special-endpoint
            br
            | You choose your own URL, nothing auto-generated here.
        .span4
          h3 Custom Responses
          p Configure JSON or XML responses for requests that specify an Accept header with application/json or application/xml. Useful for stubbing out a web hook that participates in a workflow.
        .span4
          h3 Analyze and Debug
          p View and debug the last several requests made to your URL. See headers and parameters/body information.

    #explain
      .container
        .row
          .span12.title
            h1 What is respondto.it?
        .steps
          .row
            .span12
              p.heading
                | Some services expose 
                a href="https://www.google.com/search?q=web+hook" web hooks
                | .
          .row
            .span12
              img src=url("/services-eg.png")
          .row
            .offset4
              p.heading You know, where you give them a URL to hit.
          .row
            .span12.right
              img src=url("/services-config-eg.png")
          .row
            .span7
              p.heading Sometimes you want to inspect the POST before you point the hook to your app,
          .row
            .offset4
              p.heading or you want to test out what happens if you respond with {something},
          .row
            .span8
              p.heading
                | but making a website just to test a response is work you 
                em shouldn't have to do.
    #solve
      .container
        .row
          .span12.title
            h1 That's where we come in.
        .steps
          .row
            .span12
              p.heading Create a new endpoint,
          .row
            .span12
              img src=url("/respondtoit-home-eg.png")
          .row
            .offset5.span7
              p.heading (optionally) configure some canned JSON or XML to be returned,
          .row
            .span12.right
              img src=url("/json-xml-eg.png")
          .row
            .span5
              p.heading and then point your service to us and inspect the requests.
          .row
            .span12
              img src=url("/github-config-eg.png")
    #get-started
      .container
        .row
          .span12.title
            h1
              | Okay, got it? 
              a#goto-top href='#top' Let's get started!
              br
              br
              /span.rotated style='display: inline-block;' &#x27B2;
              i.icon-chevron-up

      footer
        p
          | © Aaron Lerch 2012 
          | &nbsp;|&nbsp;
          a href='http://twitter.com/aaronlerch' twitter
          | &nbsp;|&nbsp;
          a href='http://github.com/aaronlerch' github

      a href="http://github.com/aaronlerch/respond-to-it"
        img style="position: absolute; top: 0; right: 0; border: 0;" src="https://s3.amazonaws.com/github/ribbons/forkme_right_darkblue_121621.png" alt="Fork me on GitHub"